<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.xancl.sf.cli"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <permission
        android:name="com.xancl.sf.permission.share"
        android:label="Sharing Files Permission"
        android:protectionLevel="dangerous"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!--android:permission="com.xancl.sf.permission.share"-->
        <receiver
            android:name=".SFReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="com.xancl.sf.permission.share"
            >
            <intent-filter>
                <action android:name="xancl.intent.action.share_files"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <!--<data android:scheme="content"/>-->
                <!--<data android:scheme="file"/>-->
            </intent-filter>
        </receiver>

    </application>

</manifest>